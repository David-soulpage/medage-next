import Link from "next/link";

import { Navbar, Nav, Container, Button } from "react-bootstrap";

function MarketingLayoutLight(props: any) {
  return <>{props.children}</>;
}

export default MarketingLayoutLight;
